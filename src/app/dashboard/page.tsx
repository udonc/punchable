import type { NextPage } from "next";

const page: NextPage = () => {
	return (
		<div className="grid place-items-center min-h-screen p-4">
			<p className="text-primary/50">ここにはなにもないよ</p>
		</div>
	);
};

export default page;
